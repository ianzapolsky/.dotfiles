#!/bin/sh
#
# A script that automates the process of commiting all changes and pushing to the master
# branch of a git repository. 
# by Ian Zapolsky (8/17/13)
#
# Useful when working with blogs where we most often push posts to master.
#
# Usage:
#
# gup 'commit message'

set -e

cd ~/journal
rvm use ruby-1.9.3
jekyll serve --port 8000 --watch
